apiVersion: v1
kind: Service
metadata:
  name: aisoc-api
  namespace: aisoc
  labels:
    app: aisoc-api
spec:
  type: ClusterIP
  selector:
    app: aisoc-api
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
