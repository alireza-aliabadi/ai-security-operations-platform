1. AISOC has been installed as release {{ .Release.Name }}.

2. Frontend ingress host:
   http{{ if .Values.ingress.tls }}s{{ end }}://{{ .Values.ingress.host }}/

3. API is reachable at:
   http{{ if .Values.ingress.tls }}s{{ end }}://{{ .Values.ingress.host }}/api/v1/

4. In-cluster services:
   - {{ include "aisoc.fullname" . }}-api:{{ .Values.api.service.port }}
   - {{ include "aisoc.fullname" . }}-frontend:{{ .Values.frontend.service.port }}
   - {{ include "aisoc.fullname" . }}-mcp:{{ .Values.mcp.service.port }}

5. Postgres / Redis / Qdrant are expected as external services by default
   (values.postgres.external / redis.external / qdrant.external = true).
   Override connection settings and secrets before production use.

6. Verify:
   kubectl get pods,svc,ingress -l app.kubernetes.io/instance={{ .Release.Name }}
