{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "aisoc.fullname" . }}
  labels:
    {{- include "aisoc.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: {{ include "aisoc.fullname" . }}-api
                port:
                  number: {{ .Values.api.service.port }}
          - path: /health
            pathType: Prefix
            backend:
              service:
                name: {{ include "aisoc.fullname" . }}-api
                port:
                  number: {{ .Values.api.service.port }}
          - path: /metrics
            pathType: Exact
            backend:
              service:
                name: {{ include "aisoc.fullname" . }}-api
                port:
                  number: {{ .Values.api.service.port }}
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "aisoc.fullname" . }}-frontend
                port:
                  number: {{ .Values.frontend.service.port }}
  {{- if .Values.ingress.tls }}
  tls:
    - hosts:
        - {{ .Values.ingress.host | quote }}
      secretName: {{ include "aisoc.fullname" . }}-tls
  {{- end }}
{{- end }}
